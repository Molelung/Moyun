# 诗云（Daily You）极简诗意改造计划

> 目标：把界面复杂、加载卡顿的日记应用，改造成「一言」式的极简诗意应用——
> 打开即是白纸一张，一句诗，一个加号。

---

## 一、现状诊断

### 1.1 界面问题
- 底部 3 个标签页（首页日历 / 画廊 / 统计）+ 设置页，信息密度过高
- 首页堆叠了：支持横幅、回忆卡片横滑、整月日历、可展开 FAB、通知引导弹窗
- 界面风格偏 Material 通用组件，缺乏辨识度

### 1.2 卡顿根因（性能）
| 问题 | 位置 | 说明 |
| --- | --- | --- |
| 启动全量加载 | `main.dart` / `AppDatabase.open` | 启动即加载全部 entries/images/tags/templates 到内存 |
| 首页每次 build 全量重算 | `home_page.dart` → `FlashbackManager.getFlashbacks` | 每次重建都遍历全部条目算回忆 |
| 整月日历渲染 | `vertical_calendar.dart`（838 行） | 一次性渲染整月单元格 |
| 三页同时构建 | `mobile_scaffold.dart` 的 PageView/IndexedStack | 画廊+统计页与首页共存于内存 |
| 图片无缩略图 | `local_image_loader.dart` | 原图直接解码，无尺寸适配 |
| 错误提示延迟 | `backup_restore_utils.dart` / `export_utils.dart` | catch 里 `Future.delayed(5s)` 阻塞流程 |
| 字体文件巨大 | `SotyrFangsong-S2T-Regular.ttf`（22MB） | 整包加载 |

### 1.3 已有的诗意基础（应保留发扬）
- 宣纸色主题：背景 `#F7F4ED`，墨色文字 `#2C2A29`
- 楷书字体 XuandongKaishu、仿宋字体 SotyrFangsong（中文字体，自带古韵）
- 竖排/书卷排版是核心视觉语言，见第五节

---

## 二、已完成的工作（本会话已落地，作为计划基础）

| 项 | 内容 | 状态 |
| --- | --- | --- |
| 依赖版本统一 | pubspec.yaml 全部依赖补齐版本约束；SDK 加 `<4.0.0` 上界；`shared_storage` 从 `ref: release` 固定到精确 commit | ✅ 已做 |
| 前后端统一 | Kotlin 插件统一到 `com.demizo.daily_you` 包并移入对应目录；删除非法 import/死代码 `MediaScannerPlugin.kt`；MainActivity 清理 | ✅ 已做 |
| Gradle 统一 | 各模块 Kotlin JVM target 自动对齐 Java target，解决 shared_storage 等旧依赖编译失败 | ✅ 已做 |
| 后端可读性重构 | notification_manager 双单例清除、entries_provider 命名元组/现代 switch、3 处语言解析去重、zip_utils 类型化请求等 11 个文件 | ✅ 已做 |
| 应用图标 | 新图标 `Gemini_Generated_Image_t4cjunt4cjunt4cj.png` 已放入 `assets/` 并重新生成全平台图标 | ✅ 已做 |
| 英文字体 | Courgette（C 开头花体手写体）已复制到 `assets/fonts/` 并注册 pubspec，主题全局生效（中文回退仿宋→楷书） | ✅ 已做 |
| 诗意组件（未接线） | `paper_texture.dart`（程序化宣纸纹理）、`vertical_text.dart`（右到左竖排文字）、`keyword_cloud.dart` + `word_cloud.dart`（关键词提取+词云）——已编译通过，待接入界面 | ✅ 已做（待接线） |

---

## 三、改造目标（一句话）

> **打开应用 = 一张宣纸 + 一段竖排文字 + 一个加号。**
> 上滑看往昔，下滑看统计，像翻开一本自己的书。

---

## 四、信息架构：去掉底部 3 个标签页

### 4.1 删除
- ❌ 底部导航栏（首页/画廊/统计 3 个标签页）
- ❌ 首页整月日历（`vertical_calendar.dart` 不再使用）
- ❌ 画廊页 `gallery_page.dart`（含图片网格、搜索、排序弹窗）
- ❌ 统计页旧版 `statistics_page.dart`（含大量心情图表）
- ❌ 支持横幅、可展开 FAB（`expressive_fab_menu.dart`）、通知引导弹窗
- ❌ 设置页中的心情图标定制区块

### 4.2 保留 / 重构
- ✅ 写日记流程：`edit_entry_page.dart`（去除心情选择器后继续使用，支持一天多篇、图片、标签）
- ✅ 日记详情页：`entry_view_page.dart`（去除心情图标显示）
- ✅ 设置页：保留全部功能入口，入口收进首页右上角小图标
- ✅ 数据层（数据库/DAO/Provider）完全不动，零迁移风险
- ✅ 回忆（On This Day）通知逻辑保留（数据驱动）

### 4.3 新结构
```
首页（诗意卡片）  ← 上滑 ── 时间轴页（Flomo 式竖时间线）
     │
     └── 下滑 ── 统计页（字数 + 关键词词云）
```
- 二级页：写日记、日记详情、设置（常规 push）

---

## 五、诗意首页设计（核心）

### 5.1 宣纸背景
- 首选：程序化宣纸纹理（已写好 `PaperTexturePainter`：暖纸色渐变 + 纤维噪点 + 斑点 + 暗角，seed 固定不闪烁）
- 备选：后续可替换为真实宣纸照片素材（放进 `assets/`，一行代码切换）
- 色板：纸白 `#F7F4ED` / 墨色 `#2C2A29` / 朱砂点缀（印章感）

### 5.2 磨砂玻璃卡片（主页唯一焦点）
- `BackdropFilter` 高斯模糊 + 半透明白 + 细白描边，圆角 24
- 卡片内容（优先级从高到低）：
  1. **今天已写日记** → 显示今天最新一篇
  2. **今天未写** → 显示随机过去一天的日记（按日期播种随机，每天固定，像一言每天一句）
  3. **从未写过** → 诗意占位文案（如「今日无事，唯有清风」类，可本地化）
- 卡片顶部小字：日期（如「癸卯年·八月三日」风格，可先用现有 `TimeManager.formatDate`）
- 点击卡片 → 打开该日记详情

### 5.3 竖排文字（古代书卷排版）
- 已写好 `VerticalText` 组件：字符自上而下、列从右到左，标点居中，段间空列，超长截断
- 首页卡片正文使用竖排；**编辑页保持横排输入**（竖排编辑器涉及自定义输入法管线，列为远期可选，见风险）
- 字体：正文仿宋，标题/日期用楷书（Courgette 负责英文）

### 5.4 悬浮控件
- 单个磨砂玻璃 **+** FAB（右下）：点击 → 写今天的日记（多篇可继续添加）
- 设置小按钮（右上，与现在主页同位置）：进入设置

### 5.5 手势
- 首页**上滑** → 时间轴页（滑入动画）
- 首页**下滑** → 统计页
- 时间轴页下滑/返回键 → 回首页

---

## 六、时间轴页（Flomo 风格）

### 6.1 布局
- 宣纸背景延续
- 居中一条**竖直线**，每个日期一个圆点节点，按天分组（日期头 + 当日多条日记）
- 日记文本横排（可读性优先），点击进详情
- 一直向上滑动看全部历史；向下滑/返回回到首页

### 6.2 时间跳转悬浮按钮（右缘竖排小按钮组）
- 「今天」「一月」「三月」「一年」+ 一个日期选择器按钮
- 点击后滚动到对应时间点
- 实现建议：引入 `scrollable_positioned_list`（变高列表按索引跳转的标准方案）；数据按天分组为列表项

---

## 七、统计页（极简）

### 7.1 字数统计（只留这类）
- 总篇数 / 总字数 / 记录天数 / 平均每日字数
- 简洁卡片或一行行排版，无图表堆砌

### 7.2 关键词词云（核心亮点）
- 已写好 `extractKeywords`：英文按词切分（去停用词），中文按双字 bigram 近似分词（免词典依赖）
- 已写好 `WordCloud`：按频次决定字号/字重/颜色，确定性布局不跳动
- 显示 Top N（默认 40）关键词，点击某词可（可选）筛选相关日记

### 7.3 完全移除
- 心情统计、心情图表、心情分布、心情趋势——全部移除

---

## 八、心情组件全面移除清单

| 文件 | 处理 |
| --- | --- |
| `widgets/entry_mood_picker.dart` | 从编辑页移除使用 |
| `widgets/mood_icon.dart` | 从详情页/卡片移除显示 |
| `widgets/mood_summary_chart.dart` | 随统计页重写移除 |
| `pages/edit_entry_page.dart` | 删除心情选择器、`mood` 状态、心情变更逻辑 |
| `pages/entry_view_page.dart` | 删除心情图标 |
| `pages/settings/appearance_settings.dart` | 删除「更改心情图标」区块 |
| `providers/entries_provider.dart` | 删除 `OrderBy.mood` 排序分支（数据字段保留） |
| `flashback_manager.dart` | 「美好回忆/排除坏心情」逻辑**保留**（数据驱动，mood 字段仍存在） |

> 注意：`Entry.mood` 字段与数据库列**保留不动**（删除列需要迁移，风险大于收益；界面不再出现即可）。

---

## 九、性能优化（治卡顿）

1. **启动提速**：`AppDatabase.open` 不再一次全量加载 → 分页/按需加载；图片列表懒加载
2. **首页重算缓存**：回忆/随机卡片计算结果缓存，避免每次 `notifyListeners` 全量重算
3. **图片缩略图**：`ImageStorage.getBytes` 前先生成适配尺寸缩略图（`flutter_image_compress` 已在依赖中）
4. **移除三页同构**：去掉 PageView/IndexedStack，单页常驻（架构上自然解决）
5. **字体瘦身**：仿宋 22MB → 子集化（可用 `fontTools` 或在线工具切出常用字），首屏明显加速
6. **去掉 5 秒错误延迟**：备份/导出失败时改为立即返回 + SnackBar 提示
7. **竖排渲染优化**：`VerticalText` 内按字符缓存 `TextPainter`，避免每帧重建

---

## 十、实施阶段（每阶段结束可构建验证）

| 阶段 | 内容 | 涉及文件 |
| --- | --- | --- |
| P0 基础（已部分完成） | 版本/前后端/后端重构/图标/字体/4 个组件 | 见第二节 |
| P1 导航骨架 | 去掉底部 3 标签，`MobileScaffold` 改为单页；删横幅/日历/FAB 菜单引用 | `mobile_scaffold.dart`、`main.dart` |
| P2 诗意首页 | 宣纸背景 + 磨砂玻璃卡片 + 竖排正文 + 单个 FAB + 设置按钮 + 随机/今日逻辑 | 重写 `home_page.dart` |
| P3 手势 + 时间轴 | 上滑/下滑导航；新建 `timeline_page.dart`（竖时间线 + 跳转按钮组）；引入 `scrollable_positioned_list` | 新 `pages/timeline_page.dart` |
| P4 统计页 | 重写为字数统计 + 词云；接线 `extractKeywords`/`WordCloud` | 重写 `statistics_page.dart` |
| P5 心情清理 | 按第八节清单移除所有心情 UI | 编辑页/详情页/卡片/设置页/排序 |
| P6 性能优化 | 按第九节逐项落地 | 启动/图片/缓存/字体 |
| P7 文案与回归 | 诗意占位文案本地化（zh/en 兜底）；`flutter analyze` 0 错误；`flutter build apk --debug --flavor independent` 打包；装手机验证；老数据完整 | 全项目 |

---

## 十一、风险与回退

- **纯 UI 层改造**：数据库、模型、Provider 数据流不动，已有日记数据零风险
- **竖排输入**：本期编辑页保持横排（显示竖排）；竖排编辑器需自定义软键盘管线，列为远期可选项
- **词云分词精度**：bigram 是近似方案，若后续追求准确可接入 jieba 词典包（可配置开关）
- **回退**：git 分支 `poetic-redesign` 上实施，随时可 `git checkout` 回退；每个阶段独立提交

---

## 十二、验收标准（DoD）

- [ ] 打开应用：宣纸背景，仅见一张磨砂玻璃卡片 + 一个加号 + 角落设置钮
- [ ] 今天写过 → 卡片显示今天最新一篇（竖排）；没写过 → 显示随机一天（每天固定）
- [ ] 点 + → 可写日记（横排输入），可一天多篇；写完首页卡片自动换成最新
- [ ] 上滑 → 竖时间轴，一直滑到最早；右缘按钮可跳「今天/一月/三月/一年/选日期」
- [ ] 下滑 → 统计页：字数类统计 + 关键词词云；无任何心情元素
- [ ] 界面无任何心情图标/选择器/统计；设置页无心情区块
- [ ] `flutter analyze` 0 error；debug APK 打包成功；安装手机（`com.demizo.daily_you.debug`）不覆盖原版
- [ ] 全程冷启动明显变快，滑动无明显掉帧
